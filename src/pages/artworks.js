import React from "react";
import Layout from "../components/Layout";

const Artworks = () => {
    return (
        <Layout>
            <div></div>
        </Layout>
    );
};

export default Artworks;
